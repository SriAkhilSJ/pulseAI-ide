The furniture_site landing page (test/furniture_site) is completely blank in the browser when built and served — nothing renders at all, not even the navbar. This is a real bug in the current code, not a missing dependency or build config issue (the build itself succeeds with no errors). Find the root cause and fix it. Verify your fix actually works by rebuilding and rendering the page (e.g. with test_local_html on the built dist/index.html), not just by reading the code.
